open_project -reset $project_name
add_files $cpp_name
add_files -tb $testbench_name
set_top $top_function
open_solution -reset "solution1"
set_part {$part}
create_clock -period $clock_period -name default
csim_design
csynth_design
exit

