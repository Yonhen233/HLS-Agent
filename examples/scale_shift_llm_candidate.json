{
  "task_type": "operator",
  "op_type": "ScaleShift",
  "name": "scale_shift_llm",
  "input_shape": [16],
  "output_shape": [16],
  "dtype": "ap_fixed<16,6>",
  "top_function": "scale_shift_llm",
  "target": {
    "backend": "VivadoHLS",
    "part": "xc7z020clg400-1",
    "clock_period": 8
  },
  "optimization": {
    "objective": "latency",
    "pipeline_ii": 1
  },
  "max_repair_attempts": 6,
  "llm_candidate": {
    "required": true,
    "max_repair_attempts": 6,
    "reason": "ScaleShift is intentionally not covered by hls4ml or local fallback templates; this demo exercises the LLM candidate generation and verification path."
  },
  "candidate_contract": {
    "top_function": "scale_shift_llm",
    "required_files": [
      "candidate/scale_shift_llm.h",
      "candidate/scale_shift_llm.cpp",
      "candidate/testbench.cpp"
    ],
    "signature": "void scale_shift_llm(data_t input[16], data_t output[16])",
    "dtype": "typedef ap_fixed<16,6> data_t",
    "operation": "for each i in [0, 15], output[i] = input[i] * 2 + 1",
    "testbench": {
      "input_pattern": "input[i] = i - 8",
      "expected_formula": "expected = input[i] * 2 + 1",
      "success_marker": "GOLDEN_CHECK_PASSED",
      "failure_marker": "GOLDEN_CHECK_FAILED",
      "tolerance": 0.001
    },
    "hls_constraints": [
      "Use static arrays only.",
      "Include ap_fixed.h in the header.",
      "Do not use dynamic allocation, file I/O, system calls, exceptions, threads, STL containers, or networking.",
      "Use Vivado HLS 2018.3 compatible C++."
    ]
  },
  "demo": {
    "expected_path": "llm_candidate_path",
    "mock_tools": false,
    "description": "Dedicated LLM candidate demo: unsupported ScaleShift operator should be generated by LLM, verified with testbench, and synthesized by real Vivado HLS when configured."
  }
}
